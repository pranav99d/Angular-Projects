import { Active1Directive } from './active1.directive';

describe('Active1Directive', () => {
  it('should create an instance', () => {
    const directive = new Active1Directive();
    expect(directive).toBeTruthy();
  });
});
